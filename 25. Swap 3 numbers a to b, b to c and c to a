#include <stdio.h>

int main() {
    int a = 1, b = 2, c = 3;
    printf("Before swapping: a = %d, b = %d, c = %d\n", a, b, c);
    
    int temp;
    temp = a;
    a = b;
    b = c;
    c = temp;
    
    printf("After swapping: a = %d, b = %d, c = %d\n", a, b, c);
    return 0;
}
